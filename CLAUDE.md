# AI INSIGHTS デイリー(AI業界ニュースポッドキャスト)

「今日のエピソードを作って」と言われたら、以下の手順でエピソードを1本作成する。
台本はLLM APIではなくClaude Code自身が書く(コスト回避のためのユーザー方針)。

## 手順

1. **リサーチ**: AI業界の直近24時間のニュース・話題を収集する
2. **台本執筆**: `scripts/pending.json` に `{"title", "description", "script"}` のJSONで保存
3. **生成実行**: `python main.py`(venvが必要。requirements.txt参照)
   - Fish Audio(モデル `s2.1-pro-free` 固定)で音声化 → `docs/audio/` に保存
   - `docs/feed.xml` 更新、台本は `scripts/published/` へ自動アーカイブ
4. **確認**: 生成されたMP3をユーザーに届ける

## リサーチのルール(daily-news-automationリポジトリの運用ルール準拠)

- **鮮度が最重要**: 原則として直近24時間以内の情報のみ。各記事の公開日時を必ず確認する。前日に扱った話題の再掲は禁止(新しい進展があるときのみ可)。前日の内容は `scripts/published/` で確認できる。
- **情報源**(優先順):
  - Google News RSS検索(`.env` の `NEWS_FEED_URLS`、`topic_fetcher.py` で取得可)
  - ITmedia AI+、日経クロステック、Publickey、ASCII.jp
  - 各社公式ブログ: OpenAI / Anthropic / Google / Meta
  - Hacker News 上位のAI関連トピック
  - 英語記事は和訳して採用可(参照元URLは原文のまま記録)
- **広めのクエリでも検索**: 「生成AI 発表」「LLM 新モデル」「AI 資金調達」「AI 規制」等でも検索し、当日の新規トピックを漏らさない。
- **話題系も扱う**: X等で盛り上がっているAIツールの使い方、モデルの評判、面白い活用事例なども番組の柱。ニュースが少ない日は活用Tips系の話題でつなぐ。
- **事実確認**: 記事本文や公式発表で裏付けを取ってから台本に書く。憶測を事実のように語らない。未確認情報は「〜と報じられています」と出典を明示する。

## 台本のトーン

- テック好きのリスナーに話しかける、親しみのある「です・ます」調の1人語り
- 目安1200〜2000文字(音声3〜5分)
- 読み上げ用テキストのみ(見出し・記号・効果音指示は入れない)
- クロージングは「それではまた明日、AIの最前線でお会いしましょう」系の一言で締める

## 読み仮名辞書(pronunciation_dict.json)

- TTSが読み間違える固有名詞(サービス名・企業名・専門用語など)は `pronunciation_dict.json` に「表記→読み仮名」で登録する。音声生成時に自動置換される(台本JSON自体は元表記のままでよい)。
- ユーザーから読み間違いの指摘があったら、必ずこの辞書に追記すること。
- 新しいモデル名・企業名を台本に使うときは、読みが自明でなければ辞書に登録してから生成する。

## 技術メモ

- 実行環境: プロジェクト内 `venv`(なければ `python3 -m venv venv` で作成して `pip install -r requirements.txt`)
- feedparserは直接URLを渡すとSSLエラーになる環境があるため、`topic_fetcher.py` はrequests経由で取得している
- ローカルにffmpegがないため、複数チャンクの結合はバイト連結フォールバックで動く
- Fish Audioモデルは `s2.1-pro-free` 固定(完全無料・フェアユース)。変更禁止
